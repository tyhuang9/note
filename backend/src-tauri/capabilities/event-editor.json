{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "event-editor",
  "description": "Restricted future event-editor capability. It may only receive sanitized native events.",
  "windows": ["event-editor"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten"
  ]
}
