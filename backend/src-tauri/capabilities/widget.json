{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "widget",
  "description": "Restricted agenda widget capability. It may read only the bounded widget agenda DTO and receive sanitized native events.",
  "windows": ["widget"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "allow-calendar-widget-agenda"
  ]
}
