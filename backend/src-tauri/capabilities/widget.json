{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "widget",
  "description": "Restricted agenda widget capability. It may read only the bounded widget agenda DTO, control its own safe shell state, navigate to the main calendar, and receive sanitized native events.",
  "windows": ["widget"],
  "permissions": [
    "core:event:allow-listen",
    "core:event:allow-unlisten",
    "allow-calendar-widget-agenda",
    "allow-widget-status-get",
    "allow-widget-show",
    "allow-widget-hide",
    "allow-widget-toggle",
    "allow-widget-set-locked",
    "allow-widget-set-size-preset",
    "allow-widget-open-calendar"
  ]
}
