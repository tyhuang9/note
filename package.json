{
  "name": "note-workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview",
    "tauri:dev": "cd backend && npm --prefix ../frontend exec tauri -- dev",
    "tauri:build": "cd backend && npm --prefix ../frontend exec tauri -- build"
  }
}
